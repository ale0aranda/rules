name: Quality

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  check:
    name: Check rules
    uses: ./.github/workflows/reusable-check.yml
    with:
      node-version: "24"
